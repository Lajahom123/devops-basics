apiVersion: v1
kind: Service
metadata:
  name: devops-tracker-api
  namespace: devops-tracker
spec:
  type: ClusterIP
  selector:
    app: devops-tracker-api
  ports:
    - name: http
      port: 80
      targetPort: 3000