name: Postgres Entra bootstrap

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AKS_CLUSTER_NAME: aks-devops-tracker-dev-vsh6qw-swn
  RESOURCE_GROUP: rg-devops-tracker-dev-runtime
  JOBS_RELEASE_NAME: devops-tracker-jobs
  NAMESPACE: devops-tracker
  CHART_PATH: helm/jobs/devops-tracker-jobs
  VALUES_FILE: helm/jobs/devops-tracker-jobs/values-dev.yaml
  BOOTSTRAP_JOB_NAME: devops-tracker-db-bootstrap
  ACR_NAME: devopstrackerdevvsh6qw
  ACR_LOGIN_SERVER: devopstrackerdevvsh6qw.azurecr.io

jobs:
  bootstrap:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_DEV_GITHUB_ACTIONS_DEPLOY_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: ACR login
        run: az acr login --name "$ACR_NAME"

      - name: Build and push bootstrap image
        run: |
          IMAGE_TAG="postgres-bootstrap-${GITHUB_SHA}"
          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"

          docker buildx build \
            --platform linux/amd64 \
            -f Dockerfile.postgres-bootstrap \
            -t "$ACR_LOGIN_SERVER/devops-tracker-ops:$IMAGE_TAG" \
            --push \
            .

      - name: Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group "$RESOURCE_GROUP" \
            --name "$AKS_CLUSTER_NAME" \
            --overwrite-existing

      - name: Delete previous bootstrap job
        run: |
          kubectl delete job "$BOOTSTRAP_JOB_NAME" \
            -n "$NAMESPACE" \
            --ignore-not-found

      - name: Enable bootstrap job
        run: |
          helm upgrade "$JOBS_RELEASE_NAME" "$CHART_PATH" \
            --install \
            --namespace "$NAMESPACE" \
            -f "$VALUES_FILE" \
            --wait \
            --timeout 2m \
            --set migrations.enabled=false \
            --set bootstrap.enabled=true \
            --set-string bootstrap.image.tag="$IMAGE_TAG" \
            --set-string bootstrap.image.pullPolicy=Always \
            --set-string global.tenantId="${{ vars.AZURE_TENANT_ID }}" \
            --set-string bootstrap.managedIdentityClientId="${{ vars.AZURE_DEV_POSTGRES_BOOTSTRAP_CLIENT_ID }}"

      - name: Wait for bootstrap job
        run: |
          kubectl wait \
            --for=condition=complete \
            --timeout=2m \
            "job/$BOOTSTRAP_JOB_NAME" \
            -n "$NAMESPACE"

      - name: Print bootstrap logs
        if: always()
        run: |
          kubectl logs \
            -n "$NAMESPACE" \
            "job/$BOOTSTRAP_JOB_NAME" \
            --tail=-1 || true

      - name: Disable bootstrap job
        if: always()
        run: |
          helm upgrade "$JOBS_RELEASE_NAME" "$CHART_PATH" \
            --install \
            --namespace "$NAMESPACE" \
            -f "$VALUES_FILE" \
            --wait \
            --timeout 2m \
            --set bootstrap.enabled=false \
            --set migrations.enabled=false \
            --set-string global.tenantId="${{ vars.AZURE_TENANT_ID }}"