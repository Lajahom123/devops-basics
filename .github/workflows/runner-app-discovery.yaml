name: Check private app endpoint

on:
  workflow_dispatch:

jobs:
  private-validation:
    runs-on:
      - self-hosted
      - vnet

    environment: dev

    steps:
      - name: Validate private app endpoint
        env:
          APP_HEALTH_URL: "https://devops-tracker-29193.azurewebsites.net/health"
        run: |
          curl --fail --silent --show-error "$APP_HEALTH_URL"